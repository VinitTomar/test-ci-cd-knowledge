name: My workflow

on: push

jobs:
  check-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Get main branch commit
        id: getSinceCommit
        run: |
            commit=$(git merge-base HEAD origin/${{ github.event.repository.default_branch }})
            echo "::set-output name=sinceCommit::${commit}"
      - name: Print main branch commit
        run: echo "${{ steps.getSinceCommit.outputs.sinceCommit }}"
      - name: Get current branch commit
        run: echo $(git log -1 | grep "^commit" | awk '{print $2}')


