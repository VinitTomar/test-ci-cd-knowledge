name: My workflow

on: push

# uses: ./.github/workflows/diff-checker.yml

jobs:
  Diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      with:
        fetch-depth: 0
      - uses: ./.github/actions/diff-checker

